- [Sheet template 2026-07](references/sheet-template-2026-07.md) — ops sheet moved to index 0 (~2026-07-28) so `get_worksheet(1)`/`WorksheetIndex=1` started hitting `Инфо`; data table also shifted to B..M from row 21. FIXED + DEPLOYED 2026-07-31 (`165e590`, `205741f`): both writers resolve the tab BY TITLE and anchor the append range to the "Сана" header (`B20:K`); Fly bot confirmed live in its logs. Health check: `L1` "Oxirgi sinxron" must keep advancing. Still open: install the new `.exe` on the accountant's PC (old build still writes to `Инфо`), clear `Инфо`!L216:R216 + I217:R233, re-enter the 16 lost transactions (dedup won't replay them), decide what goes in E/F.
- [Park API credentials](references/park-api-credentials.md) — Yandex Fleet park creds in .env (PARK_CLID/PARK_API_KEY/PARK_ID), not yet used by code
- [Fleet API transactions](references/fleet-api-transactions.md) — how to pull park transactions via Yandex Fleet API (endpoints + header auth mapping, verified schema)
- [MultiCard WalletHistory API](references/multicard-wallethistory-api.md) — 2nd tx source (multiavto.uz): login/data endpoints, tiyin units. GEO-BLOCKED (UZ-IP only; creds OK). SPLIT OUT of the Telegram app; then BUILT as a C#/.NET 10/Avalonia Windows desktop app in `multicard/` (runs on accountant's UZ PC: startup + periodic sync + dedup + UI). Builds via .github/workflows/multicard-build.yml (needs 3 GitHub Secrets). Fly app stays Telegram-only. 2026-07-10: desktop app gained ⬇️Fetch preview (PreviewAsync, no write) + a **per-row 📤 Yozish button** (`WriteOneAsync`, idempotent, baseline-safe: doesn't advance lastSync on first run) + a live 📜Log tab (Serilog UI sink); sheet rows now leave Инфо(D)/Изоҳ(J) EMPTY for both credit & debit (accountant request). 11/11 tests, build clean; not driven live (dev Mac UZ IP → would hit real API/Sheet). Note also covers the **`.exe` upgrade procedure** — state lives in `%APPDATA%\MultiCardSync\` (safe to swap the binary), but the startup registry entry is never rewritten once set, so replace in place or the old build keeps auto-starting.
